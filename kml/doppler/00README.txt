http://radar.weather.gov/ridge/kmzgenerator.php

I removed NWS and NOAA logos. The above generator always gives same filename to KMZ, so 
can't reference URL directly for the different map types, which is why I download here.

These are supposed to refresh every 2 minutes and pull the new image off of their
website. But the refresh only seems to work in Google Earth, not Google Maps API.
However, when Google Map is re-loaded, should pull the new map.
